apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
  namespace: YIT PLACEMENT
spec:
  replicas: 1
  selector:
    matchLabels:
      app: frontend
  template:
    metadata:
      labels:
        app: frontend
    spec:
      containers:
        - name: frontend
          image: yit-placement-frontend:1.0
          ports:
            - containerPort: 80
          envFrom:
            - configMapRef:
                name: frontend-config

